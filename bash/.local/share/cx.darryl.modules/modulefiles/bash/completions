#%Module

module-help {Enable bash completion framework}

depends-on brew/shellenv

if {[module-info shell] eq "bash"} {
    source-sh bash [file join [getenv HOMEBREW_PREFIX] etc profile.d bash_completion.sh]
}
