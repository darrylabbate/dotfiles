# Displays red prompt if root
# Displays blue prompt during SSH session

if [ $(id -u) -eq 0 ];
then
	PS1="\[\e[0;31m\][\h:\u]\[\e[m\] \w \$ "
elif [ -n "$SSH_CLIENT" ];
then
	PS1="\[\e[0;34m\][\h:\u]\[\e[m\] \w \$ " 
else
	PS1="\w \$ "
fi