#%Module

module-help {User environment configuration}

set modules_prefix [file join /opt homebrew opt modules]
set completion_file [file join $modules_prefix init bash_completion]

source-sh bash $completion_file

set-alias m ml
